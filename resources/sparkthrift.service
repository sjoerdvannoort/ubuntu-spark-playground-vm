[Unit]
Description=Spark deamons
After=derbyns.service

[Service]
User=root
Type=forking
ExecStart=bash /opt/sparkplayground/sbin/start-thrift.sh
ExecStop=bash /opt/sparkplayground/sbin/stop-thrift.sh

[Install]
WantedBy=multi-user.target
